diff --git a/gradle.properties b/gradle.properties
index acc8d49..6ef9b31 100644
--- a/gradle.properties
+++ b/gradle.properties
@@ -4,6 +4,6 @@ org.gradle.parallel=true
 org.gradle.configuration-cache=false
 
 # Versions
-mod_version=1.0.0
+mod_version=1.0.0-beta.1+worldfile.4
 velocity_api_version=3.4.0-SNAPSHOT
 limboapi_version=1.1.27
diff --git a/src/main/java/fr/jeanney/limboautoserver/Configuration.java b/src/main/java/fr/jeanney/limboautoserver/Configuration.java
index 270304d..89aa031 100644
--- a/src/main/java/fr/jeanney/limboautoserver/Configuration.java
+++ b/src/main/java/fr/jeanney/limboautoserver/Configuration.java
@@ -175,6 +175,54 @@ public class Configuration {
         return config.getDouble("limbo.pitch", 0.0);
     }
 
+    /**
+     * Path to a schematic/structure pasted into the limbo world at startup.
+     * Empty (default) keeps the bare void world.
+     */
+    public String getLimboWorldFile() {
+        return config.getString("limbo.worldFile", "");
+    }
+
+    /**
+     * World file format: SCHEMATIC (legacy .schematic), WORLDEDIT_SCHEM
+     * (modern .schem) or STRUCTURE (vanilla structure block .nbt).
+     */
+    public String getLimboWorldFileType() {
+        return config.getString("limbo.worldFileType", "WORLDEDIT_SCHEM").toUpperCase();
+    }
+
+    public int getLimboWorldFileOffsetX() {
+        return config.getLong("limbo.worldFileOffsetX", 0L).intValue();
+    }
+
+    public int getLimboWorldFileOffsetY() {
+        return config.getLong("limbo.worldFileOffsetY", 90L).intValue();
+    }
+
+    public int getLimboWorldFileOffsetZ() {
+        return config.getLong("limbo.worldFileOffsetZ", 0L).intValue();
+    }
+
+    public int getLimboWorldFileLightLevel() {
+        return config.getLong("limbo.worldFileLightLevel", 15L).intValue();
+    }
+
+    /**
+     * Allow players to walk around the limbo (default false = upstream
+     * behaviour: pinned in place via disableFalling).
+     */
+    public boolean isLimboMovementAllowed() {
+        return config.getBoolean("limbo.allowMovement", false);
+    }
+
+    /**
+     * With allowMovement: players falling below this Y are teleported back to
+     * the spawn point (walked off the pasted build into the void).
+     */
+    public double getLimboRespawnBelowY() {
+        return config.getDouble("limbo.respawnBelowY", -64.0);
+    }
+
     // ----- helpers -----
 
     private String serverKey(RegisteredServer server, String key) {
diff --git a/src/main/java/fr/jeanney/limboautoserver/LimboAutoServer.java b/src/main/java/fr/jeanney/limboautoserver/LimboAutoServer.java
index 2e8b8c0..d9c8c5f 100644
--- a/src/main/java/fr/jeanney/limboautoserver/LimboAutoServer.java
+++ b/src/main/java/fr/jeanney/limboautoserver/LimboAutoServer.java
@@ -41,6 +41,7 @@ public class LimboAutoServer {
     private final ProxyServer proxy;
     private final PluginLogger logger;
     private final Configuration config;
+    private final StartupHistory startupHistory;
 
     private ServerManager serverManager;
     private LimboManager limboManager;
@@ -51,6 +52,7 @@ public class LimboAutoServer {
         this.proxy = proxy;
         this.config = new Configuration(dataDirectory);
         this.logger = new PluginLogger(this, logger);
+        this.startupHistory = new StartupHistory(dataDirectory, this.logger);
     }
 
     @Subscribe
@@ -216,4 +218,8 @@ public class LimboAutoServer {
     public ServerManager getServerManager() {
         return serverManager;
     }
+
+    public StartupHistory getStartupHistory() {
+        return startupHistory;
+    }
 }
diff --git a/src/main/java/fr/jeanney/limboautoserver/LimboManager.java b/src/main/java/fr/jeanney/limboautoserver/LimboManager.java
index 997a66b..f702d51 100644
--- a/src/main/java/fr/jeanney/limboautoserver/LimboManager.java
+++ b/src/main/java/fr/jeanney/limboautoserver/LimboManager.java
@@ -7,8 +7,13 @@ import net.elytrium.limboapi.api.Limbo;
 import net.elytrium.limboapi.api.LimboFactory;
 import net.elytrium.limboapi.api.chunk.Dimension;
 import net.elytrium.limboapi.api.chunk.VirtualWorld;
+import net.elytrium.limboapi.api.file.BuiltInWorldFileType;
+import net.elytrium.limboapi.api.file.WorldFile;
 import net.elytrium.limboapi.api.player.GameMode;
 
+import java.nio.file.Files;
+import java.nio.file.Path;
+
 /**
  * Owns the LimboAPI virtual world/server that players wait in while their
  * backend boots, and handles spawning players into it.
@@ -56,6 +61,8 @@ public class LimboManager {
                 config.getLimboPosX(), config.getLimboPosY(), config.getLimboPosZ(),
                 (float) config.getLimboYaw(), (float) config.getLimboPitch());
 
+        pasteWorldFile(config, world);
+
         Limbo newLimbo = factory.createLimbo(world)
                 .setName("LimboAutoServer")
                 .setWorldTime(config.getLimboWorldTime())
@@ -73,6 +80,67 @@ public class LimboManager {
         logger.info("Limbo world ready (dimension={}, gameMode={}).", dimension, gameMode);
     }
 
+    /**
+     * Pastes the configured schematic/structure into the limbo world.
+     *
+     * <p>Fail-open: any problem (missing file, unknown type, parse error) is
+     * logged and the limbo stays a bare void world — a bad world file must
+     * never take the whole proxy hostage.</p>
+     */
+    private void pasteWorldFile(Configuration config, VirtualWorld world) {
+        try {
+            doPasteWorldFile(config, world);
+        } catch (Exception e) {
+            // toml4j getters hard-cast TOML values (e.g. `worldFile = true` throws
+            // ClassCastException before any specific handling below can run).
+            logger.error("Failed to paste limbo world file; keeping empty world.", e);
+        }
+    }
+
+    private void doPasteWorldFile(Configuration config, VirtualWorld world) {
+        String pathString = config.getLimboWorldFile();
+        if (pathString == null || pathString.isEmpty()) {
+            return;
+        }
+
+        Path path;
+        try {
+            path = Path.of(pathString);
+        } catch (java.nio.file.InvalidPathException e) {
+            logger.error("Invalid limbo worldFile path '{}'; keeping empty world.", pathString);
+            return;
+        }
+        if (!Files.isRegularFile(path)) {
+            logger.error("Limbo world file '{}' not found; keeping empty world.", pathString);
+            return;
+        }
+
+        BuiltInWorldFileType type;
+        try {
+            type = BuiltInWorldFileType.valueOf(config.getLimboWorldFileType());
+        } catch (IllegalArgumentException e) {
+            logger.error("Unknown limbo worldFileType '{}' (expected SCHEMATIC, WORLDEDIT_SCHEM or STRUCTURE); keeping empty world.",
+                    config.getLimboWorldFileType());
+            return;
+        }
+
+        try {
+            WorldFile worldFile = factory.openWorldFile(type, path);
+            worldFile.toWorld(factory, world,
+                    config.getLimboWorldFileOffsetX(),
+                    config.getLimboWorldFileOffsetY(),
+                    config.getLimboWorldFileOffsetZ(),
+                    Math.max(0, Math.min(15, config.getLimboWorldFileLightLevel())));
+            logger.info("Pasted limbo world file '{}' ({}) at offset ({}, {}, {}).",
+                    pathString, type,
+                    config.getLimboWorldFileOffsetX(),
+                    config.getLimboWorldFileOffsetY(),
+                    config.getLimboWorldFileOffsetZ());
+        } catch (Exception e) {
+            logger.error("Failed to paste limbo world file '{}'; keeping empty world.", pathString, e);
+        }
+    }
+
     /**
      * Spawns the player into the limbo world and kicks off the backend startup.
      */
diff --git a/src/main/java/fr/jeanney/limboautoserver/ServerManager.java b/src/main/java/fr/jeanney/limboautoserver/ServerManager.java
index 289bcbb..76e30f4 100644
--- a/src/main/java/fr/jeanney/limboautoserver/ServerManager.java
+++ b/src/main/java/fr/jeanney/limboautoserver/ServerManager.java
@@ -55,12 +55,15 @@ public class ServerManager {
                 if (isOnline) {
                     return CompletableFuture.completedFuture(null);
                 }
+                long bootStart = System.currentTimeMillis();
                 return runStart(server)
                         .thenCompose(result -> waitForServerToBecomeResponsive(server))
                         .thenAccept(isResponsive -> {
                             if (!isResponsive) {
                                 throw new CompletionException(new RuntimeException("Server started but is not responsive."));
                             }
+                            // 量測「發動啟動 -> 首次 ping 通」餵給滾動歷史(boss bar 估值)
+                            plugin.getStartupHistory().record(name, System.currentTimeMillis() - bootStart);
                         });
             });
 
diff --git a/src/main/java/fr/jeanney/limboautoserver/StartupSessionHandler.java b/src/main/java/fr/jeanney/limboautoserver/StartupSessionHandler.java
index d65b4e1..b79bce2 100644
--- a/src/main/java/fr/jeanney/limboautoserver/StartupSessionHandler.java
+++ b/src/main/java/fr/jeanney/limboautoserver/StartupSessionHandler.java
@@ -29,6 +29,7 @@ public class StartupSessionHandler implements LimboSessionHandler {
     private BossBar bossBar;
     private ScheduledTask progressTask;
     private long startedAt;
+    private long lastVoidRespawn;
     private boolean transferring;
     private volatile boolean finished;
 
@@ -44,7 +45,13 @@ public class StartupSessionHandler implements LimboSessionHandler {
     public void onSpawn(Limbo server, LimboPlayer limboPlayer) {
         this.limboPlayer = limboPlayer;
         this.startedAt = System.currentTimeMillis();
-        limboPlayer.disableFalling();
+        if (plugin.getConfig().isLimboMovementAllowed()) {
+            // Let the player walk around the pasted world; onMove() below
+            // recovers anyone who falls off the build into the void.
+            limboPlayer.enableFalling();
+        } else {
+            limboPlayer.disableFalling();
+        }
 
         ServerManager serverManager = plugin.getServerManager();
         serverManager.cancelShutdownServer(target);
@@ -73,6 +80,28 @@ public class StartupSessionHandler implements LimboSessionHandler {
         });
     }
 
+    @Override
+    public void onMove(double posX, double posY, double posZ) {
+        if (limboPlayer == null || !plugin.getConfig().isLimboMovementAllowed()) {
+            return;
+        }
+        if (posY < plugin.getConfig().getLimboRespawnBelowY()) {
+            long now = System.currentTimeMillis();
+            if (now - lastVoidRespawn < 1000L) {
+                return;   // 墜落中的移動包連發,teleport 一次即可
+            }
+            lastVoidRespawn = now;
+            Configuration config = plugin.getConfig();
+            limboPlayer.teleport(config.getLimboPosX(), config.getLimboPosY(), config.getLimboPosZ(),
+                    (float) config.getLimboYaw(), (float) config.getLimboPitch());
+        }
+    }
+
+    @Override
+    public void onMove(double posX, double posY, double posZ, float yaw, float pitch) {
+        this.onMove(posX, posY, posZ);
+    }
+
     private void transferToServer() {
         if (transferring || !player.isActive()) {
             return;
@@ -93,7 +122,9 @@ public class StartupSessionHandler implements LimboSessionHandler {
         bossBar = BossBar.bossBar(progressTitle(0), 0f, BossBar.Color.BLUE, BossBar.Overlay.PROGRESS);
         player.showBossBar(bossBar);
 
-        long estMillis = Math.max(1000L, plugin.getConfig().getEstimatedStartupSeconds() * 1000L);
+        // 估值優先用實測滾動平均(近 5 次開機),無歷史才退回設定檔
+        long estMillis = Math.max(1000L, plugin.getStartupHistory().averageMillis(targetName)
+                .orElse(plugin.getConfig().getEstimatedStartupSeconds() * 1000L));
         progressTask = plugin.getProxy().getScheduler()
                 .buildTask(plugin, () -> {
                     if (finished || bossBar == null || !player.isActive()) {
diff --git a/src/main/resources/config.toml b/src/main/resources/config.toml
index a3c17f1..bca6573 100644
--- a/src/main/resources/config.toml
+++ b/src/main/resources/config.toml
@@ -34,6 +34,19 @@ pitch = 0.0
 # readTimeout/2 ms, so this MUST stay well under the vanilla client's 30s timeout,
 # independently of Velocity's global read-timeout. 20000 -> keep-alive every 10s.
 readTimeout = 20000
+# Optional schematic/structure pasted into the limbo at startup (empty = void).
+# worldFileType: SCHEMATIC (legacy .schematic) / WORLDEDIT_SCHEM (modern .schem) / STRUCTURE (vanilla .nbt)
+# Offsets place the file's origin corner in the world; keep the build under posY so players spawn on it.
+worldFile = ""
+worldFileType = "WORLDEDIT_SCHEM"
+worldFileOffsetX = 0
+worldFileOffsetY = 90
+worldFileOffsetZ = 0
+worldFileLightLevel = 15
+# Let players walk around the limbo (false = upstream behaviour: pinned in place).
+# With movement on, anyone falling below respawnBelowY is teleported back to spawn.
+allowMovement = false
+respawnBelowY = -64.0
 # Show a boss bar that fills while the backend starts.
 progressBar = true
 # Rough estimate (seconds) of how long the backend takes to boot. Only drives how
