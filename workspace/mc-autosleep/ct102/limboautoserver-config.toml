# CT102 /opt/velocity/plugins/limboautoserver/config.toml(mc-autosleep 2026-07-10)
# 玩家 join 停機後端 -> 進 proxy 內建等待室 -> start 指令(mc-gate.sh -> CT100 hook -> MCSM open)
# -> 後端 ping 通自動送入;無人 600s(使用者裁決 10 分鐘)-> stop 指令同路徑休眠。

maintenanceInterval = 5
responsivenessTimeout = 1000

[logging]
level = "INFO"

[limbo]
dimension = "OVERWORLD"
gameMode = "ADVENTURE"
worldTime = 1000
# 出生點=Modern House 室內地板(貼圖原點 0,64,0;NBT 算出中心柱室內地板 schem y=14 → 站面 79;
# 使用者 2026-07-10:屋頂出生改室內)
posX = 38.5
posY = 79.2
posZ = 59.5
yaw = 0.0
pitch = 0.0
# 等待室建築(mc-autosleep worldfile 補丁;Modern House by Raaamseeel,abfielder id=8717,
# 76x46x118,Sponge schem v2 DataVersion 4325)
worldFile = "/opt/velocity/plugins/limboautoserver/modern-house.schem"
worldFileType = "WORLDEDIT_SCHEM"
worldFileOffsetX = 0
worldFileOffsetY = 64
worldFileOffsetZ = 0
worldFileLightLevel = 15
# 等待期間可走動逛房子;掉出建築(y<40,地面在 64)自動拉回天台出生點
allowMovement = true
respawnBelowY = 40.0
readTimeout = 20000
progressBar = true
# boss bar 估值:worldfile.4 起優先用實測滾動平均(近 5 次開機,存 plugins/limboautoserver/
# startup-history.txt),本值只在無歷史時墊底;實際送入仍等真 ping
estimatedStartupSeconds = 22
progressTitle = "<gold>伺服器喚醒中…</gold> <gray>(%time%s)</gray>"
progressReadyTitle = "<green>就緒,進入中!</green>"

[messages]
prefix = "<gray>[</gray><gold>24Bay</gold><gray>]</gray> "
starting = "<yellow>伺服器喚醒中,請稍候(約 1 分鐘;逢每日 03:10 備份會多等片刻)…</yellow>"
failed = "<red>伺服器啟動失敗,請通知管理員。</red>"
notify = "<green>伺服器就緒,正在送入…</green>"

[servers]

[servers.mc]
workingDirectory = "./"
start = "bash /opt/velocity/mc-gate.sh start"
stop = "bash /opt/velocity/mc-gate.sh stop"
startupDelay = 5
shutdownDelay = 60
# 無人 10 分鐘自動休眠(使用者裁決 2026-07-10)
autoShutdownDelay = 600
autoShutdownRequiresEmptyProxy = true
preserveQuotes = true
